import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ch.candy.chase',
  appName: 'candy-chase',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
